<div class="top-show-challenge">
  <div class="mx-3">
    <div class="col-12 d-flex">
      <div class="container">

          <%# CAS: challenge terminé // DONE %>
          <% if @challenge.started %>
            <% if @challenge.end_date < Date.today %>
              <div class='challenge-description my-4 d-flex flex-column align-items-center'>
                <div class="mt-5 mb-2">
                  <h2 class="card-blue">"<%= @challenge.title %>" est terminé</h2>
                </div>
              </div>
                <div>
                  <h6 class="pb-3">Terminé depuis le <%= @challenge.end_date %>,<br>Durée totale : <%= @challenge.total_time %> jours.</h6>
                  <div class="card3 text-center my-4">
                    <p>🎉 Tu as effectué <%= @progression_count_current_user %> jours de challenge, et remporté <%= @total_points_count %> points! 🎉</p>
                  </div>
                <p class="my-4"><em>Nostalgique ? Tu peux toujours recréer ce challenge, ou en inventer un autre <%= link_to "ici", new_challenge_path %>!<em></p>
                </div>
      </div>
    </div>
  </div>
</div>

<div class="top-show-challenge">
  <div class="mx-3">
    <div class="col-12 d-flex">
      <div class="container">

            <%# CAS: challenge lancé, en cours %>
            <% else %>
              <div class='challenge-description mt-5 d-flex flex-column align-items-center'>
                <div class="mt-5 mb-2">
                  <h1 class="card-blue">"<%= @challenge.title.capitalize %>" est lancé!</h1>
                </div>
                <div class="card-under-blue my-5">
                  <p> Wouhou! Tu participes actuellement à ce challenge !</p>
                  <i class="fas fa-rocket i1"></i>
                  <i class="fas fa-rocket i2"></i>
                  <i class="fas fa-rocket i3"></i>
                  <p class="text-center my-2"><strong>Date de fin:</strong> <%= ConvertDate.new(date: @challenge.end_date.strftime('%a %d %b')).call %>.</p>
                  <p>Il te reste <%= @time_left %> jours pour te surpasser!</p>
                </div>
                <p class="blockquote mt-2">"<%= @challenge.description %>"</p>
                <div class="card4 text-center my-4">
                  <h5>Tous les participant·e·s</h5>
                  <ul class="text-left">
                    <% @users.each do |user| %>
                      <li><%= cl_image_tag user.photo.key, id:"avatar" %></li>
                      <li><small><%= user.name.capitalize %></small></li>
                      <li>
                        <% @participations.each do |participation| %>
                          <% if participation.user_id == user.id %>
                            <% @progression_count = 0 %>

                            <% participation.progressions.each do |progression| %>
                              <% if progression.done %>
                                <% @progression_count += progression.points %>
                              <% end %>
                            <% end %>
                          <% end %>
                        <% end %>
                      </li>
                    <% end %>
                  </ul>
                </div>
      </div>
    </div>
  </div>
</div>
                <%# <div class="card-under_blue"> %>
                <%# </div> %>
                  <%# <div class="card-not-center my-4"> %>
                    <ul class="mb-0">

                        <div class="bottom-challenge pb-2 pt-5">

                      <% @participations.each do |participation| %>
                          <% @progression_count = 0 %>
                          <% participation.progressions.each do |progression| %>
                            <% if progression.done %>
                              <% @progression_count += 1 %>
                            <% end %>
                          <% end %>
                          <h5 class="text-white text-center pt-4 pb-3">Progressions</h5>
                          <% ratio = @progression_count*100/@challenge.total_time%>
                          <div class="mb-4">
                          <small class="mb-0 text-left mr-4"><%= participation.user.name %>: <%= @progression_count %> j </small>
                            <span class="pr-2"><%= image_tag "#{ratio.round(-1)}.svg" %></span>
                          </div>

                        </div>

                      <% end %>
                    </ul>
                  <%# </div> %>
              </div>
            <% end %>

<div class="top-show-challenge">
  <div class="mx-3">
    <div class="col-12 d-flex">
      <div class="container">

          <%# CAS: challenge prêt à être lancé, invitation amis ! FIXER le bouton accueil %>
          <% else %>
          <div class='challenge-description my-5 d-flex flex-column align-items-center'>
            <div class="mt-5 mb-2">
                <h2 id="title", class="card-blue" data-challenge="<%= @challenge.title %>">
                  "<%= @challenge.title %>" est prêt à être lancé!
                </h2>
            </div>
              <div class="card-not-center mt-4">
                <p
                  id="debut"
                  data-challenge-debut="<%= @challenge.total_time %>"
                  data-challenge-time-left="<%= @time_left %>">
                  Début: <em><%= @challenge.start_date %></em>,<br><strong>Durée:</strong> <%= @challenge.total_time %> jours.
                </p>
              </div>
                <% if current_user == @creator %>
                  <p>"<%= @challenge.description %>"</p>

                  <div class="card3 text-center my-4"
                    data-controller="share"
                    data-share-id-value= <%= @challenge.id %>
                    data-share-title-value= <%= @challenge.title %>
                    data-share-creator-value= <%= @creator.name %>>
                      <p class="pb-3">T'es prêt à récolter des points et défier tes potes?</p>
                      <p class= "btn btn-primary" data-action="click->share#shareLink"> Partage ton lien! </p>
                  </div>
              <% else %>
                <div class="card3 text-center my-4">
                  <div data-controller='enter-challenge'
                    data-enter-challenge-target="cardEnter"
                    data-enter-challenge-create-url-value= "<%= participations_path %>"
                    data-enter-challenge-user-id-value= "<%= current_user.id %>"
                    data-enter-challenge-challenge-id-value= "<%= @challenge.id %>">

                    <%# CAS: invitation reçue au challenge %>
                    <p data-enter-challenge-target="canard">Veux-tu y participer?</p>
                    <button data-enter-challenge-target="poule" class="btn btn-go btn-primary" data-action="click->enter-challenge#accept">
                      Let's go!
                    </button>
                    <button data-enter-challenge-target="oie" class="btn btn-nop btn-primary" data-action="click->enter-challenge#refuse">
                      Nope
                    </button>
                  </div>
                </div>
              <% end %>
            <div class="card3 my-3">
              <h5>Participant·e·s inscrits:</h5>
              <p class="pb-2"><em>Tout le monde est la? Lance le challenge!</em></p>
              <% @users.each do |user| %>
                <ul class="text-left in-line">
                  <li><%= cl_image_tag user.photo.key, height: 200, width: 200, crop: :fill, id:"avatar" %> <strong><%= user.name %></strong></li>
                </ul>
              <% end %>

                <%# CAS: bouton "lancer le challenge" %>
              <% if current_user == @creator %>
                <div data-controller="started"
                  data-started-update-url-value="<%=  started_challenge_path(@challenge) %>">
                  <button id="launch2" class="btn mt-2 btn-primary" data-action="click->started#status">
                    C'est parti !🚀
                  </button>
                </div>
              <% end %>
            <% end %>
          </div>
      </div>
    </div>
  </div>
</div>
